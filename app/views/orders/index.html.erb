<h1>Listing orders</h1>

<table class="table table-condesed table-striped">
  <thead>
    <tr>
      <th>Produto</th>
      <th>Quantidade</th>
      <th>Preço</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.product.name %></td>
        <td><%= order.amount %></td>
        <td><%= order.product.price %></td>
        <td><%= link_to 'Show', order,  class: "btn btn-success"  %>
        <%= link_to 'Edit', edit_order_path(order),  class: "btn btn-primary"  %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'New Order', new_order_path,  class: "btn btn-primary" %>